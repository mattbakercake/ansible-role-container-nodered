# Ansible role to install NodeRed

Install latest stable NodeRed container onto target host

# Variables

see `defaults\main.yaml`

`timezone` - local timezone
`docker_dir` - docker dir
`docker_compose_dir`: - docker compose dir